# Copia este archivo a .env y ajusta los valores.
# Si el backend no está corriendo, deja VITE_MOCK_FALLBACK en "true" para
# probar el flujo completo de la UI con datos de ejemplo.

VITE_API_BASE_URL=http://localhost:8000
VITE_MOCK_FALLBACK=true